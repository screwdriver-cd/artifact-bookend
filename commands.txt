cd $SD_ARTIFACTS_DIR
find . -type f -print > manifest.txt
find . -type f -exec sh -c 'ALLOW_VERSION=071800; f=`echo $1 | sed "s/^\.\///"`; curlWrapper=$(if test $(curl --version | head -1 | awk "{print \$2}" | awk -F. "{printf \"%02d%02d%02d\", \$1, \$2, \$3}") -gt $ALLOW_VERSION; then echo "eval"; else echo "sd-step exec core/curl"; fi); encoded=$(${curlWrapper} "curl -s -w \"%{url_effective}\" --data-urlencode $f -G \"\" | sed -e \"s/^\/?//\""); curl -H "Authorization: Bearer $SD_TOKEN" -H "Content-Type: text/plain" -X PUT $STORE_URL/v1/builds/$SD_BUILD_ID/$ARTIFACTS_DIR_SUFFIX/$encoded -T ./$f' -- {} \;
