cd $SD_ARTIFACTS_DIR
find . -type f -print > manifest.txt
find . -type f -exec sh -c 'f=`echo $1 | sed "s/^\.\///"`; curlWrapper=$(if curl -s -w "%{url_effective}" --data-urlencode encodestr -G "url" | grep -i "http://url/?encodestr" > /dev/null 2>&1; then echo "eval"; else echo "sd-step exec core/curl"; fi); encoded=$(${curlWrapper} "curl -s -w \"%{url_effective}\" --data-urlencode $f -G \"\" | sed -e \"s/^\/?//\""); echo curl -H "Authorization: Bearer $SD_TOKEN" -H "Content-Type: text/plain" -X PUT $STORE_URL/v1/builds/$SD_BUILD_ID/$ARTIFACTS_DIR_SUFFIX/$encoded -T ./$f' -- {} \;
